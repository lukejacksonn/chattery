name: PR
on: pull_request
jobs:
  chatter:
    name: chatter
    runs-on: ubuntu-latest
    steps:
      - name: post comment
        uses: ./
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
